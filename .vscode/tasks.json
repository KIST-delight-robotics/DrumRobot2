{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build - DrumRobot",
            "command": "make",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}